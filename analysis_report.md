## Краткий аналитический отчет по данным Ozon

### **Структура данных:**
Датасет содержит 1.35 млн товаров от одного продавца с 5,261 уникальными категориями, организованными в глубокую иерархию до 8 уровней вложенности (в среднем 3-4 уровня). Категории распределены равномерно — топовые содержат по 300 товаров каждая. Типология товаров очень детализирована — 12,379 уникальных типов, при этом топ-5 занимают менее 3% от общего объема. Все товары имеют уникальные идентификаторы без дубликатов.

### **Обоснование двух коллекций:**
Разделение на коллекции `categories` и `products` обусловлено необходимостью нормализации иерархической структуры категорий для централизованного управления и устранения избыточности хранения путей категорий в каждом товаре. При этом коллекция `products` сохраняет денормализованные ключевые поля категорий для оптимизации операций чтения и фильтрации.

### **Преимущества денормализации:**
1. **Оптимизация производительности** — исключение дорогостоящих JOIN операций при чтении товаров с фильтрацией по категориям
2. **Ускорение поиска** — возможность быстрого поиска по полному пути категории через обычные индексы
3. **Упрощение агрегаций** — легкое построение иерархических отчетов без использования $graphLookup
4. **Кэширование данных** — часто используемая информация о категориях кэшируется вместе с товарами
5. **Устойчивость к изменениям** — обновление структуры категорий требует модификации только в одной коллекции


### **ДАЛЕЕ ПОДРОБНЕЕ**
### 📊 **Основные метрики данных:**

| Показатель | Значение | Комментарий |
|------------|----------|-------------|
| **Всего товаров** | 1,355,049 | Объем данных значительный |
| **Уникальных категорий** | 5,261 | Широкая категоризация товаров |
| **Максимальная глубина категорий** | 8 уровней | *Ранее считалось 2 уровня - ошибка анализа* |
| **Минимальная глубина** | 1 уровень | Есть корневые категории |
| **Средняя глубина** | 3.7 уровня | Большинство категорий имеют 3-4 уровня вложенности |
| **Уникальных типов товаров** | 12,379 | Высокая детализация типизации |
| **Дубликаты Offer_ID** | 0 | Все товары уникальны |

### 🎯 **Топ-5 категорий по количеству товаров:**

1. **Детские товары\Коляски и автокресла\Детские коляски\Детские прогулочные коляски** - 300 товаров
2. **Канцелярские товары\Оборудование и принадлежности для торговли\Этикет-пистолеты и счетчики** - 300 товаров
3. **Строительство и ремонт\Сантехника\Арматура и аксессуары для сантехники\Смесительные узлы** - 300 товаров
4. **Строительство и ремонт\Отделочные материалы\Напольные покрытия\Товары для ремонта напольных покрытий** - 300 товаров
5. **Аксессуары\Детские аксессуары\Аксессуары для мальчиков\Ремни и подтяжки для мальчиков** - 300 товаров

**Наблюдение:** Все топовые категории содержат по 300 товаров - вероятно, это искусственное ограничение или особенность датасета.

### 📈 **Топ-5 типов товаров (Offer_Type):**

1. **Лекарственное средство безрецептурное** - 10,167 (0.75%)
2. **Печатная книга** - 9,657 (0.71%)
3. **Лекарственное средство рецептурное** - 8,528 (0.63%)
4. **Электронная книга** - 3,382 (0.25%)
5. **Сумка** - 3,106 (0.23%)

**Важно:** Топ-5 типов занимают менее 3% от общего объема - распределение очень диверсифицированное.

### 🔍 **Ключевые инсайты:**

1. **Иерархия глубже, чем предполагалось** - 8 уровней против ожидаемых 2
2. **Отсутствие дубликатов** - строгая уникальность товаров
3. **Равномерное распределение** в топе категорий (по 300 товаров)
4. **Высокая детализация** - 12к+ типов товаров при 1.3M записей

### 🏗️ **Рекомендации по структуре MongoDB:**

#### **Обоснование двух коллекций:**

**1. Коллекция `categories` (нормализованная):**
```javascript
{
  _id: ObjectId,
  category_id: String,      // Уникальный ID
  name: String,            // Имя уровня
  full_path: String,       // Полный путь с \
  level: Number,           // Уровень (1-8)
  parent_id: ObjectId,     // Ссылка на родителя
  path_parts: Array,       // Разделенный путь для поиска
  product_count: Number,   // Количество товаров
  is_leaf: Boolean        // Является ли конечной категорией
}
```

**2. Коллекция `products` (денормализованная):**
```javascript
{
  _id: ObjectId,
  offer_id: String,       // Уникальный ID товара
  name: String,           // Название товара
  offer_type: String,     // Тип товара (12к+ вариантов)
  partner: String,        // Продавец (_ozon)
  category: {            // Денормализованная категория
    id: ObjectId,        // Ссылка на категорию
    full_path: String,   // Для быстрого поиска
    level: Number,       // Уровень категории
    ancestors: [        // Массив предков для агрегаций
      {id: ObjectId, name: String, level: Number}
    ]
  }
}
```

#### **Преимущества денормализации для данного кейса:**

1. **Оптимизация чтения** - 99% операций будут запросами товаров с фильтрацией по категориям
2. **Быстрая фильтрация** - поиск по `category.full_path` без JOIN
3. **Агрегации по иерархии** - поле `category.ancestors` позволяет легко строить деревья
4. **Устойчивость к изменениям** - обновление пути категории требует изменения только в `categories`
5. **Производительность** - отсутствие $graphLookup для глубоких иерархий

#### **Критические индексы:**
```javascript
// Для categories
db.categories.createIndex({full_path: 1})
db.categories.createIndex({level: 1})
db.categories.createIndex({parent_id: 1})

// Для products  
db.products.createIndex({offer_id: 1}, {unique: true})
db.products.createIndex({"category.full_path": 1})
db.products.createIndex({"category.level": 1})
db.products.createIndex({offer_type: 1})
db.products.createIndex({name: "text"})
```

### 📋 **Выводы для проекта:**

1. **Глубокая иерархия** (8 уровней) требует тщательного проектирования навигации
2. **Отсутствие дубликатов** упрощает бизнес-логику
3. **Высокая детализация типов** предполагает сложную фильтрацию
4. **Гибридный подход** (нормализация категорий + денормализация товаров) оптимален
5. **Materialized Path Pattern** подходит для 8-уровневой иерархии

**Резюме:** Данные Ozon представляют каталог с глубокой иерархией категорий и высокой детализацией типов товаров. Рекомендуется использовать Materialized Path Pattern для категорий с денормализацией ключевых полей в товарах для оптимизации запросов чтения.